management:
   endpoint:
      health:
        show-details: "ALWAYS"

spring:
  rabbitmq:
    host: rabbitmq
  data:
    mongodb:
      database: ethdb
      uri: mongodb://mongo-0.mongo:27017,mongodb://mongo-1.mongo:27017
